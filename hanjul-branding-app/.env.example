# 이 파일을 복사해 .env 로 만들고 값을 채우세요. (프로토타입은 없어도 MOCK 모드로 실행됩니다)

# Anthropic API 키 — 없으면 데모(MOCK) 응답으로 동작합니다.
ANTHROPIC_API_KEY=

# 사용할 모델 (배포 시 최신 모델 ID로 교체 권장)
MODEL=claude-3-5-sonnet-latest

# 포트
PORT=3000
