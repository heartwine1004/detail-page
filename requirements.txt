# 필수
yt-dlp>=2024.1.0        # 유튜브 다운로드
Flask>=3.0.0            # (선택) 웹 UI 버전

# 원어 자막(음성인식) — 2단계
faster-whisper>=1.0.0

# 나레이션 음성 — 기본 TTS(무료, 온라인)
edge-tts>=6.1.0

# 선택: '내 목소리 복제'(Coqui XTTS) — 설치 시에만 동작. torch 등 대용량
# TTS>=0.22.0

# 선택: 원본 BGM 제거(6단계 옵션)
# demucs>=4.0.0
